'use client';
import { Search } from 'lucide-react';
export function QueueFilterBar({ basePath, search = '', sort = 'booking', eventDate = '', bookingDate = '' }: { basePath: string; search?: string; sort?: string; eventDate?: string; bookingDate?: string }) {
  return <form action={basePath} className="grid gap-2 rounded-xl border border-border bg-[#fcfaf7] p-3 dark:bg-[#241e17] sm:grid-cols-[minmax(0,1fr)_170px_170px_180px]"><label className="relative"><Search className="absolute left-3 top-3 size-4 text-muted-foreground" /><input name="q" defaultValue={search} placeholder="Search customer, booking, event, or location…" onChange={(event) => event.currentTarget.form?.requestSubmit()} className="h-10 w-full rounded-lg border border-input bg-white pl-9 pr-3 text-sm outline-none focus:border-primary dark:bg-card" /></label><label className="text-[11px] font-medium text-muted-foreground">Booking date<input name="bookingDate" type="date" defaultValue={bookingDate} aria-label="Filter booking date" onChange={(event) => event.currentTarget.form?.requestSubmit()} className="mt-1 h-9 w-full rounded-lg border border-input bg-white px-2 text-sm font-normal text-foreground dark:bg-card" /></label><label className="text-[11px] font-medium text-muted-foreground">Event date<input name="eventDate" type="date" defaultValue={eventDate} aria-label="Filter event date" onChange={(event) => event.currentTarget.form?.requestSubmit()} className="mt-1 h-9 w-full rounded-lg border border-input bg-white px-2 text-sm font-normal text-foreground dark:bg-card" /></label><select name="sort" defaultValue={sort} aria-label="Sort jobs" onChange={(event) => event.currentTarget.form?.requestSubmit()} className="h-10 rounded-lg border border-input bg-white px-3 text-sm dark:bg-card"><option value="booking">Sort by Booking date</option><option value="event">Sort by Event date</option></select></form>;
}
